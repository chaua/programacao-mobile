<?xml version="1.0" encoding="utf-8"?>
<manifest
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- PERMISSÕES -->

    <!-- Solicita permissão de acesso à internet -->
    <uses-permission android:name="android.permission.INTERNET"/>

    <!-- Solicita as permissões de localização-->
    <!--
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION"/>
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION"/>
    -->

    <!-- Solicita permissão de camera -->
    <!--
    <uses-feature android:name="android.hardware.camera"/>
    -->

    <!-- APPLICATION -->
    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.CursoAndroid"
        tools:targetApi="31">

        <!-- Activity principal - Inicia a aplicação -->
        <activity
            android:name=".MainActivity"
            android:exported="true">

        </activity>

        <activity android:name=".EventoActivity" android:exported="true">
            <!-- Intenção indicando que é a activity principal -->
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Também incluímos Services, Content Providers, Broadcast Receivers -->

    </application>

</manifest>